server:
  port: 8080 


spring:
   jpa:
     hibernate:
       ddl-auto: update 
     properties:
        hibernate:
        dialect: orig.hibernate.dialect.MySQL5Dialect
  
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/reactapp
      username: root
      password: root 